version: "3"
services:
  server:
      build:
        dockerfile: ./Dockerfile
      volumes:
        - .:/app
      depends_on:
        - postgis
      ports:
        - 3000:80
      network_mode: "host"